# 第三章：数据容器深度解析

## 1. 列表 (List)
有序、可变，支持索引和切片。
```python
fruits = ["apple", "banana"]
fruits.append("cherry")
```

## 2. 字典 (Dict)
键值对存储，无序（Python 3.7+ 有序），查询速度快。
```python
info = {"name": "张三", "score": 100}
```

## 3. 元组 (Tuple) 与 集合 (Set)
- 元组是不可变的列表。
- 集合是无序且元素唯一的容器。