# 阶段三：并发编程与标准库深入

## 1. 并发基础
- 进程、线程、协程的概念。
- `threading`：锁 (Lock)、死锁避免、Queue。
- `multiprocessing`：进程池、IPC (管道/队列)。
- `concurrent.futures`：Executor 框架。

## 2. 异步编程
- `asyncio` 核心概念：事件循环、async/await。
- 异步 I/O：`aiohttp`, `aiofiles`。

## 3. 标准库深入
- `pathlib`, `logging`, `itertools`, `functools`。